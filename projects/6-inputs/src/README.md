# Requirements

Write a program that displays the mouse position (inside the window) in the terminal every frame. When the left mouse button is held down, small dots should appear at the mouse position, creating a simple “painting” effect. To make the drawing persist on screen, store all dots inside a container so they can all be drawn every frame. Pressing the R key should clear all the dots from the screen.

