# Requirements

Write a program that creates a circle centered on the screen. Scale it gradually every frame so it grows bigger at first, and after a short time, switch the direction so it shrinks instead. Make sure the window remains responsive and the animation looks smooth.

